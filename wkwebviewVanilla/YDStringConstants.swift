import Foundation

enum Scheme {
    static let normal = "https"
    static let custom = "rm"
}

enum Endpoint {
    static let hostname = "://9006113.local"  // "://www.httpbin.org"
    static let path = "/"
}
